title = "Outbound Intelligence Engine"

[extend]
useDefault = true

[allowlist]
description = "Generated dependencies and build output"
paths = [
  '''(^|/)node_modules(/|$)''',
  '''(^|/)\.next(/|$)''',
  '''(^|/)\.turbo(/|$)'''
]
